<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.let_server.domain.menuAllergy.mapper.MenuAllergyMapper">
    <insert id="save" useGeneratedKeys="true" keyProperty="menuAllergyId">
        insert into MenuAllergy(menu_allergy_id,menu_id,allergy_idx)
        values (#{menuAllergyId},#{menu.menuId},#{allergy.allergyId})
    </insert>
</mapper>